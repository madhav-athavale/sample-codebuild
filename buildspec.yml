version: 0.2
phases:
 install:
    runtime-versions:
      python: 3.11
 
  
 build:
    commands:
      - pip install -r requirements.txt -t .
  post_build:
    commands:
      - python --version
      - python lambda_function.py
artifacts:
  files:
    - '**/*'